server:
  port: 8084

spring:
  application:  
    name: mascotas-service 

  datasource:
    url: jdbc:mysql://localhost:3306/db_mascotas?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate 
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  secret: Veterinaria.2026Veterinaria.2026Veterinaria.2026