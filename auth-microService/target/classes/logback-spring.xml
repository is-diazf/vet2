<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Propiedades para incluir en el log -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- Definir un appender que escribe en consola con formato JSON -->
    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- Opcional: incluir campos personalizados -->
            <includeCallerData>true</includeCallerData>
            <includeMdcKeyName>traceId</includeMdcKeyName>
        </encoder>
    </appender>

    <!-- Appender para archivo (opcional) -->
    <appender name="FILE_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/auth-microService.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/auth-microService-%d{yyyy-MM-dd}.json</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
    </appender>

    <!-- Usar el appender JSON en lugar del estándar -->
    <root level="INFO">
        <appender-ref ref="CONSOLE_JSON"/>
        <appender-ref ref="FILE_JSON"/>
    </root>

    <!-- Puedes ajustar niveles de paquetes específicos -->
    <logger name="cl.franco.auth" level="DEBUG"/>
</configuration>